(function (window, undefined) {
    if (window && typeof window.define == "function") return;
    var define = null,
        
    
    define = function(list, callback) {
        
    };
    
})(window);